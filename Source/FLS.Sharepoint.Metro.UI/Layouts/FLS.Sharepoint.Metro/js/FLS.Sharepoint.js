Cufon.replace('#pageTitle h1, #pageTitle h2'); // Works without a selector engine
Cufon.replace('.menu-horizontal a, #breadcrumb h2', { hover: true }); // Requires a selector engine for IE 6-7, see above
